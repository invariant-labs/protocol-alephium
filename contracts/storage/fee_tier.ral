struct FeeTier {
    mut fee: U256,
    mut tickSpacing: U256
}

Abstract Contract FeeTierHelper() {
    pub fn newFeeTier(fee: U256, tickSpacing: U256) -> FeeTier {
        assert!(tickSpacing > 0 && tickSpacing <= 100, InvariantError.InvalidTickSpacing)
        assert!(fee < one(PercentageScale), InvariantError.InvalidFee)

        return FeeTier {
            fee: fee,
            tickSpacing: tickSpacing
        }
    }
}