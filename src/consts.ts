import { CLAMM, Invariant } from '../artifacts/ts'

export const {
  SqrtPriceScale,
  LiquidityScale,
  FeeGrowthScale,
  FixedPointScale,
  PercentageScale,
  TokenAmountScale,
  FixedPointDenominator,
  GlobalMaxTick,
  GlobalMinTick,
  MaxSqrtPrice,
  MinSqrtPrice,
  SearchRange,
  InvariantError
} = Invariant.consts

export const { CLAMMError } = CLAMM.consts
